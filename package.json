{
  "name": "backlog-api-skills",
  "version": "0.3.4",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test --test-concurrency=1 tests/*.test.mjs",
    "build": "npm test && npm run build:bundle:zip",
    "smoke:runtime": "node scripts/smoke-runtime.mjs",
    "sync:runtime": "node scripts/sync-backlog-api-runtime.mjs",
    "import:runtime:release": "node scripts/import-backlog-api-release.mjs",
    "build:bundle": "node scripts/build-skill-bundle.mjs",
    "build:bundle:zip": "node scripts/build-skill-bundle-zip.mjs"
  }
}
